# 睡前故事 H5 阅读器 — 拼音功能开发与优化记录

## 概述

为睡前故事 H5 阅读器（index.html）添加拼音注音功能，面向 4 岁以下（胎教期 / 0-1 岁 / 1-3 岁）的故事，自动在中文汉字上方显示拼音。英文故事不显示拼音按钮。

---

## 一、拼音功能初版实现

### 1.1 引入 pinyin-pro 库

通过 CDN 引入 `pinyin-pro@3.26.0`：

```html
<script src="https://unpkg.com/pinyin-pro@3.26.0/dist/index.js"></script>
```

### 1.2 拼音切换按钮

在阅读器右上角添加按钮：

```html
<div class="pinyin-toggle" id="pinyinToggle">拼音</div>
```

CSS 样式：

```css
.pinyin-toggle {
  position: absolute;
  top: 16px;
  right: 16px;
  /* 按钮样式 */
}
```

### 1.3 拼音渲染函数（初版 — 逐字调用）

```javascript
function addPinyinToText(text) {
  if (!window.pinyinPro) return escapeHtml(text);
  let result = '';
  for (const char of text) {
    if (/[\u4e00-\u9fff]/.test(char)) {
      const py = pinyinPro.pinyin(char, { toneType: 'symbol', type: 'string' });
      result += '<ruby>' + char + '<rt>' + py + '</rt></ruby>';
    } else {
      result += escapeHtml(char);
    }
  }
  return result;
}
```

### 1.4 HTML ruby 标签 CSS

```css
ruby {
  ruby-position: over;
  ruby-align: center;
}
rt {
  font-size: 10px;
  font-weight: 400;
  color: var(--text-light);
  line-height: 1.2;
  font-family: -apple-system, "PingFang SC", sans-serif;
}
```

### 1.5 年龄段自动开启逻辑

```javascript
let pinyinEnabled = false;
const youngAgeGroups = ['prenatal', '0-1', '1-3'];

// 在 openStory(id) 函数中：
// 自动为低年龄段开启拼音
if (youngAgeGroups.includes(story.ageGroup)) {
  pinyinEnabled = true;
  pinyinToggle.classList.add('active');
} else {
  pinyinEnabled = false;
  pinyinToggle.classList.remove('active');
}

// 拼音切换按钮点击事件
pinyinToggle.addEventListener('click', () => {
  pinyinEnabled = !pinyinEnabled;
  pinyinToggle.classList.toggle('active', pinyinEnabled);
  // 重新渲染当前故事
  renderStoryContent();
});
```

### 1.6 初版效果

- 胎教期 / 0-1 岁 / 1-3 岁故事自动开启拼音
- 3-6 岁 / 6 岁以上默认关闭，可手动开启
- 英文故事不显示拼音按钮

---

## 二、字间距增大（防止拼音重叠）

### 2.1 问题

初版中相邻汉字的拼音标注会挤在一起，导致拼音无法清晰分辨。

### 2.2 修复

在 CSS 中增加间距，三处调整：

```css
/* 1. ruby 元素：每个带拼音的汉字后面增加右边距 */
ruby {
  ruby-position: over;
  ruby-align: center;
  margin-right: 0.15em;  /* 新增 */
}

/* 2. 正文区域：增大字间距 */
.reading-body.has-pinyin {
  font-size: 16px;
  line-height: 2.6;
  letter-spacing: 0.2em;  /* 新增 */
}

/* 3. 寓意区域：相应增大间距 */
.reading-moral-text.has-pinyin {
  font-size: 14px;
  line-height: 2.4;
  letter-spacing: 0.15em;  /* 新增 */
}
```

### 2.3 效果

- `ruby` margin-right: 0.15em — 每个带拼音汉字后留出间距
- 正文 letter-spacing: 0.2em — 字间距翻倍，拼音不再重叠
- 寓意 letter-spacing: 0.15em — 相应增大，保持视觉协调

---

## 三、多音字注音修复

### 3.1 问题

初版逐字调用 `pinyinPro.pinyin(char)` 时，库没有上下文信息，只能返回每个字的**默认读音**：

| 词语 | 逐字结果（错误） | 正确读音 |
|---|---|---|
| 着急 | zhe jí | zháo jí |
| 极了 | jí liǎo | jí le |

### 3.2 根因分析

`pinyin-pro` 库在处理多音字时，需要完整的词语上下文才能正确判断读音。逐字调用相当于每次只给库一个孤立的汉字，库只能返回该字的默认读音。

### 3.3 修复方案：整段传入

将连续的汉字组成片段，整段传入 `pinyinPro.pinyin(segment, { type: 'array' })`，让库根据词语上下文正确判断多音字：

```javascript
function addPinyinToText(text) {
  if (!window.pinyinPro) return escapeHtml(text);
  let result = '';
  let chineseBuffer = '';

  const flushBuffer = () => {
    if (chineseBuffer) {
      // 整段传入，利用上下文判断多音字
      const pinyinArray = pinyinPro.pinyin(chineseBuffer, {
        toneType: 'symbol',
        type: 'array',
      });
      for (let i = 0; i < chineseBuffer.length; i++) {
        result += '<ruby>' + chineseBuffer[i] +
          '<rt>' + (pinyinArray[i] || '') + '</rt></ruby>';
      }
      chineseBuffer = '';
    }
  };

  for (const char of text) {
    if (/[\u4e00-\u9fff]/.test(char)) {
      chineseBuffer += char;
    } else {
      flushBuffer();
      result += escapeHtml(char);
    }
  }
  flushBuffer();
  return result;
}
```

### 3.4 修复原理

| 输入方式 | 库看到的数据 | 结果 |
|---|---|---|
| 逐字：`pinyin("着")` | 单字"着"，无上下文 | zhe（默认读音） |
| 整段：`pinyin("着急")` | 完整词语"着急" | zháo jí（正确） |
| 整段：`pinyin("极了")` | 完整词语"极了" | jí le（正确） |

### 3.5 未来预防

拼音是**客户端动态生成**的（不在 `stories.json` 中存储拼音数据），所以这个修复会自动适用于所有已存在和未来的故事。不需要修改自动化任务的任何配置。

---

## 四、技术要点总结

### 4.1 技术栈

- **拼音库**：pinyin-pro@3.26.0（CDN: unpkg.com）
- **渲染方式**：HTML `<ruby>` / `<rt>` 标签
- **注音样式**：声调符号（toneType: 'symbol'），如 zháo

### 4.2 数据存储

- 拼音不存储在 stories.json 中，完全在浏览器端动态生成
- 不影响故事数据的结构，不需要修改自动化生成流程

### 4.3 年龄段自动判断

```javascript
const youngAgeGroups = ['prenatal', '0-1', '1-3'];
// prenatal（胎教期）：自动开启拼音
// 0-1（0-1岁）：自动开启拼音
// 1-3（1-3岁）：自动开启拼音
// 3-6（3-6岁）：默认关闭，可手动开启
// 6+（6岁以上）：默认关闭，可手动开启
// 英文故事：不显示拼音按钮
```

### 4.4 涉及文件

| 文件 | 修改内容 |
|---|---|
| `bedtime-story-app/index.html` | CDN 引入、按钮 HTML、CSS 样式、addPinyinToText 函数、年龄段判断逻辑 |
| `bedtime-story-app/stories.json` | 无修改（拼音为客户端动态生成） |

### 4.5 Git 提交记录

| Commit | 内容 |
|---|---|
| `c26b559` | feat: 拼音功能优化+科学故事独立系列（含多音字修复+字间距增大） |
| `ab95198` | 回退到优化前基础版 + 正确拼音（type:'all'+<ruby>，汉字拼音并排） |
| 本次 | 按开发记录重做拼音：addPinyinToText(type:'array'逐字缓冲)+按年龄段自动开启+字间距0.2em/0.15em |

---

## 五、部署与同步

每次修改后需同步以下位置：

1. **GitHub 仓库** `ldy314/bebtime-stories` — git commit & push
2. **本地** `bedtime-story-app/index.html`
3. **D 盘备份** `D:\code test\睡前故事\index.html`
4. **CloudStudio 部署** — H5 阅读器实时预览
5. **合集 HTML** — 重新生成 `collection.html`（不影响拼音功能，但需同步故事数据变更）

---

## 六、注意事项

1. **pinyin-pro 版本固定**：使用 3.26.0，不要随意升级，避免 API 变更
2. **CDN 可靠性**：unpkg.com 偶尔不稳定，可考虑备用 CDN（如 jsdelivr）
3. **escapeHtml 函数**：拼音渲染中对非汉字字符（标点、数字等）仍需 escapeHtml 处理，防止 XSS
4. **chineseBuffer 机制**：遇到非汉字字符时需要 flush buffer，确保连续汉字段完整传入
