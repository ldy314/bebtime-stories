# 2026-08-10 工作日志

## 本地 AI 多生成 5 篇多样题材胎教故事并同步 GitHub
- 用户指令：用本地 AI（不依赖 DeepSeek API）多生成 5 篇睡前故事并同步到 GitHub。
- 实现：用脚本 `scripts/add_local_ai_stories.py` 直接撰写并注入（复用 add_science_stories.py 的 4 副本注入+EMBEDDED 单行模式）。5 篇均为 prenatal（2026-08-10 < 预产期），题材刻意多样：颜色与画(2026-08-10-cn-2)/声音与音乐(2026-08-10-en-2)/味道与温暖食物(2026-08-10-cn-3)/动物朋友(2026-08-10-en-3)/四季与天气(2026-08-10-cn-4)。
- **踩坑（重要）**：首轮把第1/2篇用了 `2026-08-10-cn`/`2026-08-10-en`，但 `git push` 被拒（远端自动化已自动生成了这两个 id 的当日每日故事 b228826）。解决：5 篇统一改用同日多篇扩展 id（-cn-2～-cn-4 / -en-2/-en-3），云端先 `git reset --hard origin/main` 对齐远端（含自动化新增 2 篇），再复制到其他 3 副本抹掉旧版，重跑注入，最终 86 篇。
- 校验：4 副本均 86 篇 / 科学 10 / 我的 5 篇在列 / 无重复 id / 无蟹 / 无弯引号 / 妈妈心跳高光齐全 / EMBEDDED 单行有效；合集页重生成（86 篇）。
- 提交推送：云端 main `b228826..37d805a`（push 成功）；github-pages 镜像 `git fetch + reset --hard origin/main` 对齐到 `37d805a`。
- **可复用经验**：手动/本地补故事时，当日 base id（YYYY-MM-DD-cn/en）可能已被自动化占用，应直接用扩展 id（-cn-2/-en-2…）避免与自动化冲突；且 push 前先 `git fetch` 确认远端未领先，若领先则先对齐再叠加以免覆盖自动化产出。

## 新增《台风天的小船》胎教故事（本地 AI 撰写）
- 用户指令：今天生成一个「台风来了、城市下雨、开车变划船」的故事，优先用本地 AI。
- 实现：直接撰写《「台风天的小船」（胎教期）》，id=`2026-08-10-cn-5`（避开自动化 base cn/en 与手动 cn-2~cn-4/en-2/en-3），date=2026年8月10日·星期一，5 段，含拟声（呼呼/哗啦哗啦/咕嘟咕嘟/咚咚心跳）、未出生宝宝视角、无蟹/无弯引号。用 `scripts/inject_typhoon_story.js` 注入 4 副本 + 重写 EMBEDDED_STORIES 单行，之后 regen collection.html 到 4 副本 → 4 副本均 87 篇。
- 提示：本地 `scripts/generate-collection-html.js` 曾是不含科学章节的旧版，已用云仓权威版（输出 collection.html + science 独立章节）覆盖，collection.html 与 bedtime-story-collection.html 已对齐。

## 台风故事命名「白海豚」+ EMBEDDED_STORIES 损坏修复（2026-08-10）
- 用户指令：台风故事命名「白海豚」，更新故事。
- 实现：`2026-08-10-cn-5` 标题改为《「白海豚台风的小船」（胎教期）》，内容融入「台风白海豚从天边游来、风是尾巴、雨是水花」意象，5 段、拟声与妈妈心跳高光保留；用脚本更新 4 副本 stories.json + 重写 index.html EMBEDDED_STORIES + regen collection.html。
- ⚠️ **踩坑（重要）**：用 `indexOf(';')` 定位 EMBEDDED_STORIES 结束位置重写时，被英文故事 content 里的分号截断，导致 index.html 新旧 JSON 拼接损坏（cn-5 出现两次、JSON parse 失败）。**修复方法：改为按行替换**（`lines.findIndex(l => l.trim().startsWith('const EMBEDDED_STORIES = '))` 整行替换），4 副本均恢复合法 JSON（87 条）。
- **可复用经验**：重写 EMBEDDED_STORIES 一律按行替换（找以 `const EMBEDDED_STORIES = ` 开头的行整行重写），**绝不要用 `indexOf(';')` 定位**，因为 story content 中可能含分号导致截断。
- 推送：云端 main `8f72aa9..e8b0d96`（keepalive 重试成功）；github-pages 镜像 fetch 后 origin/main ref 仍异常，直接 `git reset --hard e8b0d96` 对齐成功。
- 云端部署（同日下午）：collection-deploy/index.html 用 D 盘权威 collection.html（含科学章节+白海豚）覆盖后部署，两个 CloudStudio 链接均更新成功（H5 阅读器 `9579701995034ab0af6d08e3b2292da7` / 合集 `268b20b7b5cb4295a01a09e5af8bf7ac`），线上验证白海豚均可见。顺手把 automation 目录的 `generate-collection-html.js` 也替换为权威版（含 science 章节），避免将来自动化重生成合集丢科学章节。

## 故事生成切换智谱 GLM-4V-Flash（替换 DeepSeek）
- 用户指令：用 zp.txt 里的智谱 API key 替换 GitHub 中的 DeepSeek key 用于生成故事，模型 GLM-4V-Flash。
- 修改：云仓 github-bedtime-stories —— `generate-story.js`(ZHIPU_API_KEY / open.bigmodel.cn / /api/paas/v4/chat/completions / glm-4v-flash)、`refresh-seeds.js`(同)、`.github/workflows/generate-stories.yml`(env ZHIPU_API_KEY)、`setup-github.sh`、`README.md`；D 盘 `scripts/gen-today.js` 同步更新；已同步到 github-pages 镜像 + D 盘 scripts/ + D 根。
- 推送：云端 main `37d805a..8f72aa9`（SSH keepalive 参数 ServerAliveInterval=15 解决 Connection reset；普通 push 偶发断连，加 GIT_SSH_COMMAND 重试成功）。github-pages 镜像 fetch 后 origin/main ref 状态异常，改用 `git reset --hard 8f72aa9`（直接 commit hash）对齐成功。
- ⚠️ **未完成项**：`gh secret set ZHIPU_API_KEY` 返回 403（fine-grained PAT 无 Actions secrets 写权限；读权限正常，旧 DEEPSEEK_API_KEY secret 仍在）。需要用户在 GitHub 网页手动添加 secret（Settings→Secrets and variables→Actions→New repository secret→ZHIPU_API_KEY），否则云端 Actions 生成会因缺 key 失败。
