# 2026-08-13 工作日志

## 确认 8/13 云端自动生成故事 + 风格符合指南（用户要求"确保风格按照已有指南"）
- 云端 Actions 凌晨已自动生成 8/13 两篇并推送（`d1a7d55 Auto-generate bedtime stories 2026-08-13`），本地 fetch 对齐，总故事数 93 篇。
- 8/13-cn《小面团暖暖的太阳之旅》（12段954字）：拟声词 9 处（叮叮当当/咕嘟咕嘟/咕噜咕噜/咯咯咯/呼噜呼噜/叮咚）、温柔高光✅（烤箱=温暖怀抱比喻）、未出生视角✅、无弯引号✅、无蟹✅、胎教期✅、主题（面团变面包）与近期（摇篮曲/雨滴）完全不同——**主题去重机制生效**。
- 8/13-en《The Sweet and Salty Zongzi》（11段1998字）：高光✅、未出生✅、无弯引号✅、无蟹✅、英文✅、胎教期✅（拟声偏少 1 处，可接受）。
- verify-style-sync：STYLE_SYNC_OK（cnThemes=6 enThemes=6 markers=23），D 盘 prompt-builder 与云仓 md5 一致。
- 已同步 4 副本（93 篇）、合集重建、推送 `d1a7d55..09fdc5d`、镜像对齐、两 CloudStudio 链接重新部署。
- 经验：每天早上的例行动作——fetch 云端（远端通常领先 1 个 Auto-generate 提交）→ 检查 8 项风格指标 → 同步 4 副本 → 重建合集 → 推送+部署。科学故事仅在 isScienceDay 出现。

## 删除 WorkBuddy 自动化后：确保每日更新继续（含当当/科学）+ 风格多变 + 本地AI批量生成
- 用户删除了 WorkBuddy 自动化，要求：①删除后每日更新继续（含当当系列+科学故事）；②风格多变（按著名作家风格加强）；③可手动发起；④今天用本地 AI 生成未来一周故事。
- **当当系列纳入云端**：新增 `scripts/generate-dangdang.js`（读 state→读指南→DeepSeek 生成 epXX.json→入库→更新 state→重建合集）+ `.github/workflows/generate-dangdang.yml`（每周五 UTC 18:00=周六凌晨2点北京，cron `0 18 * * 5`，含 workflow_dispatch）。日期逻辑用 `T00:00:00Z` + `getUTCDay`（避免 +08:00 导致星期偏移一天），WEEKDAYS 数组对齐 getUTCDay(0=周日)。下一集 ep22 将在 8/16（周六）生成。
- **风格多变**：prompt-builder.js 新增 `AUTHOR_STYLES_CN/EN`（各10种作家风格）+ `pickAuthorStyle(dateStr, lang)` 按日期确定性轮换，buildChinesePrompt/buildEnglishPrompt 的"综合风格要求"里显式指定"本篇主导作家风格+辅助风格"。实测10天7种不同主导风格。
- **本地AI批量生成未来一周**：用当前对话模型（WorkBuddy 自带模型，非 DeepSeek API）创作 14 篇——8/14~8/19 每日故事中英12篇 + 8/16 科学日中英科学故事2篇（露珠是怎么来的）。全部胎教期，符合风格（拟声词/高光轮换/未出生视角/无蟹/无弯引号），各按指定作家风格写。
- 已推送 `09fdc5d..7546574`（8f07478 当当+风格、7546574 14篇故事），4副本同步（107篇）、合集重建、两 CloudStudio 链接重新部署。
- **经验**：本地模型创作故事可直接按 stories.json 格式写 JSON（.js 后缀文件用 fs.readFileSync 而非 require 解析）；科学故事可豁免"心跳/摇篮"高光检查（有自身科普高光）。
