# 2026-08-24 工作记录

## 睡前故事：生成 9-01~9-07 七天故事（含系列）+ 同步 + 修复远程 main 被回滚

### 生成内容（19 篇，stories.json 134 → 153）
- 常规 14 篇：9-01~9-07 中英文各一篇（按 `gen_plan.js` 调 prompt-builder 提取每日题材/场合/作家风格/主角/拟声/高光后，由自带模型创作）。
- 科学系列 2 篇：9-01 是 isScienceDay（萤火虫为什么会发光，zh/en）。
- 黑猫当当 ep23《红梅公园的银杏叶》(8-22)、ep24《青果巷的月亮井》(8-29)、ep25《中秋的桂花糖芋头》(9-05)，并更新 dangdang-series-state.json（episode=26、continuity 补录 ep22-25、arc 收束为「中秋团圆·家的甜蜜」）。
- 合并脚本 `merge_all.js` 支持三类 id：regular（YYYY-MM-DD-cn/en）、science（…-science-cn/en，补 category:science/series/source）、dangdang（dangdang-epNN-zh，保留 date/episode）。

### 同步
- embed.js 嵌入 index.html（153 篇）→ 生成合集 HTML/MD → generate_missing_pages.py 补 9-01~9-07 独立页 → 复制到 github-pages/ → 提交推送。
- 三处（D 盘根 / github-pages / 远程 main）stories.json 与 index.html md5 全一致（186608a1… / 138341cc…）。

### 重要：远程 main 曾被回滚
- 首次 push 被拒，发现远程 main 被外部流程 force-push 回 8-02（56 篇），8-03 后全部内容在线上丢失（含 8-25~8-31 故事、独立页）。远程还有 rollback-a399b57 备份分支、b7d1f83 自动提交（135 篇，含自动版 dangdang-ep23《小不点的云朵摇篮》）。
- 用户确认后 `git push --force origin main`（b7d1f83...6e88c7f forced update），远程恢复为本地完整版 153 篇。本地自动版 ep23 被我们的《红梅公园的银杏叶》覆盖。
- 教训：git 本地 refs/remotes/origin/main 会过期（rev-parse 显示旧值），验证远程实际内容应优先用 `git show FETCH_HEAD:...`。

### 提醒
- 线上自动化任务（每 2 小时）可能再次 force-push main，若之后发现线上又落后，需复检。
- CloudStudio 两个云端链接仍未更新（本会话无 cloudstudio connector）。
