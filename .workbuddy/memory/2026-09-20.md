# 2026-09-20 工作记录

## 宝宝出生日变更 · 故事阶段切换 + 补齐至 9/30

**背景**：宝宝实际于 **2026-09-15** 出生（比预产期 2026-09-22 提前 7 天）。用户要求「故事进行更改，然后生成到 9月30号的故事」。

**用户拍板的三项决策**（AskUserQuestion）：
1. 切分点：**9月15日当天及之后 → 0-1 岁**；9月14日及之前保持胎教期
2. 旧故事处理：**重写为出生后口吻**（保留情节主体，改掉胎教措辞 + 改标签/标题）
3. 新增范围：9/22–9/30 **每天 4 篇（中/英日常 + 中/英科学）+ 当当连载**

**执行内容**：
- `CHILD_BIRTHDAY` 2026-09-22 → 2026-09-15，改动 prompt-builder.js / apply_series_episode.py / generate-dangdang.js 共 10 处（四副本）
- 9/15–9/21 共 28 篇改写（`scripts/rewrite_out_{a,b,c,d}.json`）；dangdang-ep27 无胎教措辞，仅改 ageGroup/ageLabel
- 新增 9/22–9/30 共 36 篇（`scripts/generate_out_{a..e}.json`）；当当 ep28《当当第一次见到小宝宝》、ep29《当当的轻手轻脚》（`scripts/generate_dangdang.json`），state → episode=30
- 注入脚本 `scripts/apply_birth_batch.py`：改写替换 + 追加 + EMBEDDED_STORIES 重写，四副本同步
- 数据 219 → 257 篇；0-1 岁阶段共 67 篇

**顺手修掉的既有 bug**：9/11–9/30 条目中文星期整体偏移 1 天。根因是 `add_batch_20260915.py` 用了 `WEEKDAYS = ['星期日','星期一',...]` 配 `datetime.weekday()`（Python 里 Monday=0），导致错位。已修正 36 条。**后续批量脚本务必用 `weekday()` 配 `['星期一',...]` 或以周日开头的数组配 `isoweekday()%7`。**

**素材生成方式**：本次未走外部 API（智谱 glm-4-flash 试跑质量不稳、glm-4.5-flash 超时）。用户指定用 **WorkBuddy 自带的 DeepSeek Flash（即对话模型本体）** 直接创作，质量最可控。已保留可复用的 API 管线脚本 `scripts/ai-0-1-pipeline.js`（复用云端 prompt-builder 构建 prompt，支持 generate / rewrite 两模式，备用）。

**推送**：
- 部署源（main）：commit `945dff0`，`C:/.../Claw/github-bedtime-stories` push → 远端 main 更新，GitHub Pages 随之构建
- 镜像：`D:/code test/睡前故事/github-pages` `git reset --hard 945dff0` 对齐
- 备份仓库（master）：`D:/code test/睡前故事` commit `f0195ab`

**同步产物**：四副本 stories.json / index.html、collection.html、bedtime-story-collection.md、单日页 bedtime-story-2026-09-11 ~ 09-30.html、dangdang-series-state.json、AGENTS.md / STORY_STYLE_GUIDE.md / README.md / dangdang-series-outline.md 的日期时间线。
