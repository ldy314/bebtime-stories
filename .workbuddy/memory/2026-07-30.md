# 2026-07-30 工作日志

## 代码审核 + 修复（Senior Developer 审核报告复核与实施）

### 备份（先于任何修改）
- 两个仓库本地提交作为回退点：`github-pages` → `3fcc387`，根目录 → `3345746`。
- 推送到 GitHub `ldy314/bebtime-stories` 的备份分支（沙箱到 GitHub 网络不稳，SSH 鉴权 OK 但传输被 reset，最终用「按哈希推送」成功）：
  - `backup/pre-fix-20260730` = github-pages 的 `3fcc387`（线上站点）
  - `backup/pre-fix-20260730-root` = 根目录的 `3345746`（全项目）
- 回退方式：`git -C <repo> checkout 3fcc387` / `3345746`，或 `git checkout backup/pre-fix-20260730[-root]`。

### 每个问题复核结论（用户要求修复前先复核是否真实）
- C2 index.html EMBEDDED_STORIES 截断：**误报**（内嵌 JSON 合法，29 篇）。跳过。
- H3 generate-story.js updateEmbeddedStories：**误报**（已有 `if (embeddedIndex === -1) throw` 防护）。跳过。
- M3 sanitizeText 单弯引号：**误报**（已处理 `\u2018/\u2019`）。跳过。
- C3 clean_stories.py 正则：**真实**，已修（加 `(science-)?`）。
- C1 stories.json 同步：当前一致，但路径未锚定脚本目录 + 无防护 → 已加固（锚定 `__file__` + 同步前一致性告警）。
- M2 generate_stories.py `import re as re2`：**真实**（多余别名），已改为 `re`。
- H1 硬编码绝对路径：**部分真实**；github-pages 关键脚本已用相对路径。根 `scripts/*.js` 多为有意跨目录引用（push-to-cloud/sync-from-cloud 指向真实云端仓库/automation 目录），故采用「优先相对路径、回退原绝对路径」的安全方案，不改变既定功能。
- M1 index.html 下拉刷新：**真实**（缺 preventDefault、scrollY===0 在 iOS 弹性滚动不稳）→ 已修（`{passive:false}` + `e.cancelable` + `scrollY<=0`），触发逻辑/阈值不变。
- H2（两份合集生成器）、H4（schema 校验）、M4（内容去重）、M5（搜索范围）：判定为非缺陷或增强项，未改动以严格保持功能不变。

### 修复后回归验证（全部通过，功能不变）
- JS 语法检查 5 个文件 OK；Python 语法检查 3 个文件 OK。
- C3 正则单测：science-cn/en 与常规 cn/en 均匹配，非法 ID 拒绝。
- 运行 generate-collection-html.js / generate-collection-md.js / verify-style-sync.js / sync_stories.py 均无错误。
- 数据校验：stories.json=29、科学故事 2 篇保留、EMBEDDED_STORIES=29、github-pages 与 root ID 完全一致。

### 本地提交
- 修复已本地提交（未推送）：因网络不稳且用户仅要求备份"之前版本"。修复分支可后续推送。

## 黑猫当当历险记（每周系列）功能落地

用户要求新增连载系列《黑猫当当历险记》（黑猫当当 + 白猫小不点 + 狸花猫八百 + 爸妈；主题含保护家人/打击坏蛋/旅游爬山/探索古迹/龙宫寻宝/智斗格格巫/寻找美食/帮助别人；强调听爸爸妈妈的话）。探险地点与科普知识点参考《博物》《环球科学》《国家地理》真实内容做儿童化改编。

### 实现要点
- 系列条目以 `series:"dangdang"` 字段写入同一份 `stories.json`（不另建库），`id` 形如 `dangdang-epNN-zh`，`episode` 连续，`source` 字段记参考杂志。
- 新增文件（automation 文件夹 + 镜像到 github-pages + D 盘 scripts/）：`series-dangdang-guide.md`（唯一创作事实来源）、`dangdang-series-state.json`（episode/continuity/arc/linkages）、`apply_series_episode.py`（追加+更新 EMBEDDED_STORIES+推进 state+重生成合集+备份）、`sync_series_cloud.js`（同步仓库+D盘+git push）。
- H5 `index.html` 新增「🐱 系列」tab + 系列过滤 + 集数角标；两个合集生成器（html/md）分离 daily 与 series，新增「黑猫当当历险记」板块；修复 TOC 集数重复 bug（加 `cleanTitle`，标题只存故事名）。
- 第1集《当当来我家》已生成（胎教期，日期 2026-07-25 周六），stories.json=30 篇，合集已重生成。

### 自动化
- 主自动化 `automation-1784174206957` 已更新：遗漏检查忽略 series 条目；第五部分新增第7步备份系列文件。
- 新建每周系列自动化 `automation-1785393137264`：FREQ=WEEKLY;BYDAY=SA，读 guide+state→写 epNN.json→apply→sync。

### 状态（更新：已解决）
- CloudStudio 阅读器(9579701995034ab0af6d08e3b2292da7) 与合集(268b20b7b5cb4295a01a09e5af8bf7ac) 已原地重新部署（含第1集）。
- D 盘备份已全量同步。
- **GitHub Pages 推送成功**：commit `8e7112d`（merge: 合并云端每日故事(科学/07-30) + 黑猫当当系列第1集）。

## 系列杂志名处理（用户澄清）
- 用户明确：博物/环球科学/国家地理只作**关键词与故事情节的幕后灵感**，故事正文与对外展示都**不出现杂志名**。
- 已做：① 系列指南两副本（automation + D盘）将"科普参考来源"改为"幕后灵感、不出杂志名、弃用 source 字段"；② 运行副本生成器移除 series 卡片的 `📚 灵感来源` 渲染（srcBadge）；③ 第1集正文 content[3] 去掉《博物》《国家地理》点名（改为"爸爸常讲…妈妈手机里存着大猫照片"）；④ 三处 stories.json + 阅读器内嵌同步。
- 校验：系列卡片本体(series-1 到 story-1)与合集 MD 系列区均无"博物/国家地理/环球科学/灵感来源"。每日科学故事徽章(🔬 科学故事·XX)为独立功能，不在本次范围，保留。
- 提交 `c3f899e` 并推送成功（远端 HEAD=c3f899e）；CloudStudio 阅读器与合集已重部署。

### 根因修复：本地与云端 main 分叉（用户"看不到更新"的真正原因）
- 现象：用户看不到更新。查因发现 `git push` 被拒——本地 `main` 比远端多「代码审查修复+系列第1集」3 提交，但云端 `main` 也往前走了十几个提交（博物/环球科学每日科学故事、风格介绍板块、时区修正、collection 生成器便携化等）。两边在 `3fcc387` 处分叉。
- 处理：`git merge origin/main`，冲突文件 stories.json/index.html/collection.html 用程序化并集合并——stories.json 取云端每日(真理源,29篇) + 本地独有系列集数(1篇) = 30 篇无重复；index.html 保留本地系列 tab 并用合并数据重生成 EMBEDDED_STORIES；collection.html 用生成器重生成。生成器脚本自动合并成功（云端修复+系列板块都在）。
- 提交 `8e7112d` 后带重试循环 `git push origin main` 成功（远端 HEAD=8e7112d）。
- **架构教训**：云端是每日故事唯一真理源，本地只额外贡献系列。今后本地系列提交前必须先 `git fetch`+`git merge origin/main` 再 push，否则会再次分叉。每周系列自动化的 `sync_series_cloud.js` 也应在 push 前先 merge。
- hy3 模型：用户反映"找不到 hy3 模型"。两个自动化 view 均不回显 modelId 字段；显式 `automation_update(modelId:"Hy3")` 成功，证明 Hy3 有效（与主自动化默认一致）。之前是创建时未持久化或 UI 瞬时问题，现已显式锁定。

## 合集页系列板块置顶修复（BugFix）
- 现象：用户在合集页看到「系列集数 1 / 第1集」入口，但系列故事正文排在 29 篇每日故事之后（页面最底部，全页约 2000 行），难以进入阅读。
- 修复：将两个 `generate-collection-html.js`（automation 运行副本 + github-pages 真理源副本）的 `${seriesBlock}` 移到 `${storyCards}` 之前（置顶）；seriesBlock 新增 `.series-section-title`（🐱 黑猫当当历险记 · 每周系列）分区标题 + 说明小字 + 对应 CSS（紫 #6b4ea0）。
- QA 独立回归：置顶✅、分区标题样式✅、系列正文5段完整✅、TOC 锚点 #series-1/#story-1 可达✅、三处文件(automation 产物 / D盘备份 / github-pages/collection.html) 字节级 IDENTICAL✅。统计项「篇故事=29 系列集数=1」为设计本意（互斥统计，非 bug），清单误写为30。
- 部署：CloudStudio 合集链接重部署成功(268b20b7b5cb4295a01a09e5af8bf7ac)；github-pages 提交 f93746f 并 push 成功（远端 HEAD=f93746f），GitHub Pages 重新构建。

## 当当人物设定校正（用户反馈）
- 用户指正：当当白毛应在**胸口**（不是尾巴尖）；他很调皮、**喜欢躲在床底下**。
- 修改：
  - 第1集《当当来我家》正文(stories.json + H5/仓库/D盘 三处 index.html EMBEDDED_STORIES)：preview 与 content[0] 的「尾巴尖白毛」→「胸口的毛里悄悄藏着一小撮白毛」；content[1] 新增当当躲床底捉迷藏的调皮段落（原床底躲猫设定只写了哥哥八百社恐躲床底，现当当也明确爱躲床底玩）。
  - 系列创作指南 `series-dangdang-guide.md`（automation + D盘 两副本）：当当外形「尾巴尖白毛」→「胸口白毛」；性格新增「喜欢嗖地钻到床底下跟爸爸妈妈玩捉迷藏」。保证后续每周自动生成一致。
  - 重生成合集(html+md)，同步三处 + collection-deploy。
- 部署：github-pages 提交 bcdc181 并 push 成功（远端 HEAD=bcdc181）；CloudStudio 阅读器(9579701995034ab0af6d08e3b2292da7) 与合集(268b20b7b5cb4295a01a09e5af8bf7ac) 已重部署。

## 家规新增「保护宝宝」（用户要求）
- 用户：家规里还要加「保护宝宝」。宝宝是家里最小最宝贝的人类小孩（预产期 2026-09-22）。
- 第1集《当当来我家》家规(content[3])追加：「还要一起保护好宝宝——宝宝是家里最小、最宝贝的小成员，三只猫都要当他的小守护。」；结尾句「来保护他的家人啦」→「来保护他的家人和宝宝啦」。
- 系列指南 `series-dangdang-guide.md`（automation 副本）：核心教育底线追加「保护宝宝（家里最小最宝贝的成员）」；爸爸妈妈规矩追加「、保护好宝宝」+ 新句「三只猫都要把保护宝宝当成最重要的任务之一」。
- 修改文件：3×stories.json + 3×index.html(EMBEDDED_STORIES) + 2×合集(bedtime-story-collection.html/github-pages/collection.html，脚本重生) + 指南 + dangdang-ep01.json 草稿。自动化目录两副本(bedtime-story-collection.html、collection-deploy/index.html)初漏改，后手动补 Edit 同步（为 CloudStudio 部署源）。
- 校验：NEW 串「还要一起保护好宝宝」命中全部预期文件；旧家规串与旧结尾串 0 残留。
- 部署：github-pages 提交 `4878565` 并 push 成功（main，fast-forward，无分叉）。CloudStudio：合集原地更新(268b20b7b5cb4295a01a09e5af8bf7ac)；阅读器先误从 D 盘建了新沙箱 8e2457...，后改从 Claw/bedtime-story-app 重部署还原原 URL(9579701995034ab0af6d08e3b2292da7)。GitHub 合集页：https://ldy314.github.io/bebtime-stories/collection.html
- 注意：scripts/generate-collection-html.js 在接手前就有约 20 行未提交改动，本次 `git add -A` 一并提交（commit 4878565 含此脚本改动）。需向用户确认该脚本改动是否符合预期。
- 架构教训固化：GitHub Pages 也需展示合集界面(collection.html)，与 CloudStudio 合集页同套；推送前务必先 fetch+merge 防分叉（本次未触发）。
